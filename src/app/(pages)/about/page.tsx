import { Box, Typography } from '@mui/material';

export default function About() {
  return (
    <Box>
      pending that all the backlog Lets start the about page design thjats all
      about that
      <Typography variant='h1'>ala re ala</Typography>
    </Box>
  );
}
